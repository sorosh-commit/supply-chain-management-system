# سند فاز ۳: شرح موارد کاربری و سناریونویسی (Use Case Descriptions & Scenarios)
## سامانه مدیریت انبار و زنجیره تأمین

---

## ۱. مورد کاربری: ثبت و مدیریت کالا (Manage Products)
* **شناسایی:** `UC-01`
* **بازیگران اصلی:** مدیر انبار / مدیر سیستم
* **هدف:** ثبت یک کالای جدید در سیستم همراه با کد SKU، قیمت، مشخصات انبارداری و دسته‌بندی.
* **پیش‌شرط‌ها:** کاربر باید در سیستم لاگین کرده و دسترسی مدیریتی داشته باشد؛ دسته‌بندی کالا و تأمین‌کننده از قبل در سیستم تعریف شده باشند.
* **پس‌شرط‌ها:** کالا با موفقیت در پایگاه داده ثبت شده و در لیست محصولات قابل مشاهده است.

### سناریوی اصلی (Main Flow):
1. کاربر از منوی سیستم گزینه «مدیریت کالاها» و سپس «افزودن محصول جدید» را انتخاب می‌کند.
2. سیستم فرم ثبت مشخصات کالا (شامل نام، کد SKU، توضیحات، واحد اندازه‌گیری، قیمت خرید و فروش، انتخاب دسته‌بندی و تأمین‌کننده) را نمایش می‌دهد.
3. کاربر اطلاعات کالا را وارد می‌کند.
4. کاربر روی دکمه «ذخیره» کلیک می‌کند.
5. سیستم اطلاعات را اعتبارسنجی کرده (یکتا بودن SKU و معتبر بودن قیمت‌ها) و در پایگاه داده ذخیره می‌کند.
6. سیستم پیغام موفقیت‌آمیز بودن ثبت کالا را به کاربر نمایش می‌دهد.

### سناریوهای جایگزین / خطا (Alternative/Exception Flows):
* **۳-الف (کد SKU تکراری):** اگر کد SKU وارد شده قبلاً برای کالای دیگری ثبت شده باشد، سیستم خطای «کد SKU تکراری است» را نمایش داده و از کاربر می‌خواهد کد جدیدی وارد کند.
* **۵-الف (نقص اطلاعات اجباری):** اگر فیلدهای ستاره‌دار (مثل نام یا قیمت) خالی رها شده باشند، سیستم خطای عدم تکمیل اطلاعات را نشان داده و عملیات متوقف می‌شود.

---

## ۲. مورد کاربری: ثبت تراکنش‌های انبار - ورود/خروج کالا (Stock Transactions)
* **شناسایی:** `UC-02`
* **بازیگران اصلی:** مدیر انبار / اپراتور لجستیک
* **هدف:** ثبت رسمی ورود یا خروج کالا به/از انبار و به‌روزرسانی اتوماتیک موجودی در جدول Stock.
* **پیش‌شرط‌ها:** انبار، کالا و کاربر فعال در سیستم وجود داشته باشند.
* **پس‌شرط‌ها:** موجودی انبار به میزان تعیین‌شده تغییر یافته و سابقه تراکنش در جدول StockMovement ثبت شود.

### سناریوی اصلی (Main Flow):
1. کاربر به بخش «تراکنش‌های انبار» رفته و گزینه «ثبت تراکنش جدید» را انتخاب می‌کند.
2. سیستم فرم مربوطه (انتخاب انبار، انتخاب کالا، نوع تراکنش اعم از ورود یا خروج، تعداد و توضیحات) را نمایش می‌دهد.
3. کاربر اطلاعات تراکنش را وارد کرده و تأیید می‌کند.
4. سیستم موجودی فعلی انبار را بررسی می‌کند (در صورت خروج کالا، چک می‌کند که آیا موجودی کافی وجود دارد یا خیر).
5. سیستم مقدار موجودی جدول Stock را به روز کرده و یک رکورد جدید در جدول StockMovement ثبت می‌کند.
6. سیستم پیام ثبت موفق تراکنش را نمایش می‌دهد.

### سناریوهای جایگزین / خطا (Alternative/Exception Flows):
* **۴-الف (کسری موجودی هنگام خروج):** اگر نوع تراکنش خروج باشد و مقدار درخواستی بیشتر از موجودی انبار (`quantity_available`) باشد، سیستم خطای «موجودی انبار ناکافی است» را صادر کرده و تراکنش لغو می‌شود.
* **۴-ب (نامعتبر بودن انبار یا کالا):** اگر انبار یا کالا غیرفعال باشد، سیستم از ثبت تراکنش جلوگیری می‌کند.

---

## ۳. مورد کاربری: ثبت و مدیریت سفارش خرید (Purchase Order - PO)
* **شناسایی:** `UC-03`
* **بازیگران اصلی:** مسئول خرید / مدیر سیستم
* **هدف:** ثبت سفارش خرید اقلام از تأمین‌کنندگان جهت تامین کسری انبار.
* **پیش‌شرط‌ها:** تأمین‌کننده و محصولات مورد نظر از قبل در سیستم تعریف شده باشند.
* **پس‌شرط‌ها:** سفارش خرید با وضعیت PENDING ثبت شده و برای تأمین‌کننده ارسال گردد.

### سناریوی اصلی (Main Flow):
1. کاربر به بخش سفارش‌های خرید رفته و گزینه «ایجاد سفارش خرید جدید» را انتخاب می‌کند.
2. سیستم فرم انتخاب تأمین‌کننده، تاریخ تحویل مورد انتظار و اقلام (محصول و تعداد) را نمایش می‌دهد.
3. کاربر تأمین‌کننده را انتخاب کرده، اقلام مورد نیاز را به همراه تعداد و قیمت واحد اضافه می‌کند.
4. کاربر روی دکمه «ثبت و ارسال سفارش» کلیک می‌کند.
5. سیستم مبلغ کل را محاسبه کرده، شماره پیگیری یکتا (PO Number) تولید می‌کند و سفارش را در دیتابیس ذخیره می‌نماید.
6. سیستم تأییدیه ثبت سفارش را نمایش می‌دهد.

### سناریوهای جایگزین / خطا (Alternative/Exception Flows):
* **۳-الف (انتخاب تأمین‌کننده غیرفعال):** اگر تأمین‌کننده انتخاب‌شده غیرفعال باشد، سیستم اخطار داده و اجازه ثبت سفارش نمی‌دهد.

---

## ۴. مورد کاربری: ثبت سفارش فروش (Customer Sales Order - SO)
* **شناسایی:** `UC-04`
* **بازیگران اصلی:** مشتری / واحد فروش
* **هدف:** ثبت سفارش خرید کالا توسط مشتری و رزرو موجودی از انبار.
* **پیش‌شرط‌ها:** مشتری در سیستم ثبت‌نام کرده و موجودی کالا در انبار کافی باشد.
* **پس‌شرط‌ها:** سفارش فروش ثبت شده، موجودی کالا رزرو شده و فاکتور اولیه آماده گردد.

### سناریوی اصلی (Main Flow):
1. کاربر (یا مشتری از طریق پنل) گزینه «ثبت سفارش فروش جدید» را انتخاب می‌کند.
2. سیستم اطلاعات مشتری و لیست محصولات قابل سفارش را نمایش می‌دهد.
3. کاربر محصولات مد نظر و تعداد را انتخاب می‌کند.
4. سیستم قیمت‌ها، تخفیف و مالیات را محاسبه کرده و مجموع مبلغ را نمایش می‌دهد.
5. کاربر سفارش را نهایی و تأیید می‌کند.
6. سیستم موجودی انبار را بررسی کرده، اقلام را رزرو کرده و شماره پیگیری سفارش فروش را صادر می‌کند.

### سناریوهای جایگزین / خطا (Alternative/Exception Flows):
* **۵-الف (عدم موجودی کافی برای اقلام سفارش):** اگر موجودی کالا برای پاسخگویی به سفارش کافی نباشد، سیستم هشدار داده و پیشنهاد می‌دهد مقدار سفارش کاهش یابد یا از انبار دیگری تأمین شود.

---

## ۵. مورد کاربری: مدیریت لجستیک و صدور بارنامه (Shipment Management)
* **شناسایی:** `UC-05`
* **بازیگران اصلی:** اپراتور لجستیک
* **هدف:** سازماندهی ارسال مرسوله به مشتری با تعیین راننده، خودرو و مسیر.
* **پیش‌شرط‌ها:** سفارش فروش تأیید شده باشد؛ راننده و خودروی آماده به کار در سیستم موجود باشند.
* **پس‌شرط‌ها:** بارنامه (Shipment) صادر شده و وضعیت سفارش به حالت در حال ارسال تغییر یابد.

### سناریوی اصلی (Main Flow):
1. اپراتور لجستیک وارد بخش ارسال‌ها شده و یک سفارش تاییدشده آماده ارسال را انتخاب می‌کند.
2. سیستم فرم تخصیص لجستیک را باز کرده و لیست رانندگان فعال، خودروهای با ظرفیت مناسب و مسیرهای تعریف‌شده را نشان می‌دهد.
3. اپراتور راننده، خودرو و مسیر را انتخاب کرده و تاریخ اعزام را ثبت می‌کند.
4. اپراتور روی دکمه «صدور بارنامه و ارسال» کلیک می‌کند.
5. سیستم بارنامه جدید با شماره یکتا ثبت کرده، وضعیت سفارش را به SHIPPED تغییر می‌دهد و اطلاعات را ذخیره می‌کند.
6. سیستم رسید یا پیش‌نمایش بارنامه را نمایش می‌دهد.

### سناریوهای جایگزین / خطا (Alternative/Exception Flows):
* **۳-الف (اشغال بودن راننده یا خودرو):** اگر راننده یا خودروی انتخاب‌شده در همان بازه زمانی در ماموریت دیگری باشد، سیستم هشدار داده و از تداخل جلوگیری می‌کند.

---

## ۶. مورد کاربری: تهیه گزارشات و ممیزی سیستم (Reports & Audit)
* **شناسایی:** `UC-06`
* **بازیگران اصلی:** مدیر سیستم / مدیر انبار
* **هدف:** دریافت گزارش‌های جامع از وضعیت موجودی، گردش مالی، تراکنش‌ها و سوابق فعالیت کاربران.
* **پیش‌شرط‌ها:** کاربر دسترسی مدیریتی داشته باشد.
* **پس‌شرط‌ها:** گزارش در قالب جدول یا فایل قابل دانلود نمایش داده شود.

### سناریوی اصلی (Main Flow):
1. کاربر به منوی گزارش‌ها مراجعه کرده و نوع گزارش (مثلاً گزارش موجودی انبار، گزارش گردش کالا، یا لاگ ممیزی) را انتخاب می‌کند.
2. کاربر بازه زمانی یا فیلترهای دلخواه (مثل نام انبار یا نوع تراکنش) را اعمال می‌کند.
3. کاربر روی دکمه «تولید گزارش» کلیک می‌کند.
4. سیستم اطلاعات مورد نظر را از پایگاه داده استخراج کرده و به صورت جدول مرتب به همراه قابلیت خروجی‌گیری نمایش می‌دهد.
